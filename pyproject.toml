[project]
name = "openclaw-on-agentcore"
version = "0.0.0"
description = "OpenClaw on Amazon Bedrock AgentCore — CDK infrastructure"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aws-cdk-lib>=2.170.0,<3.0.0",
    "constructs>=10.0.0,<11.0.0",
    "cdk-nag>=2.28.0,<3.0.0",
]

[tool.uv]
package = false
